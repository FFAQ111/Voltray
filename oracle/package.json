{
  "name": "suiwatt-oracle",
  "private": true,
  "type": "module",
  "description": "Off-chain settlement oracle for SuiWatt: reads OCPP charging sessions and settles DR rewards on Sui testnet.",
  "scripts": {
    "settle": "tsx src/run.ts",
    "settle:event": "tsx src/oracle.ts",
    "simulate": "tsx src/simulator.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "^1.45.2",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "tsx": "^4.19.2",
    "typescript": "~6.0.2"
  }
}
